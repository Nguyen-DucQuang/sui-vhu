# Example env for server-nest
DATABASE_URL=file:./dev.db
PORT_NEST=4000
ADMIN_USER=Admin
ADMIN_PASS=Admin16
# Secret used to sign short-lived admin tokens for SSE and Bearer auth
ADMIN_JWT_SECRET=please_change_this_to_a_strong_secret
GEMINI_API_KEY=YOUR_API_KEY_IF_NEEDED

# Optional: Sui RPC and private key (for server-side test transfers)
# SUI_RPC_URL should point to a testnet or devnet fullnode RPC endpoint.
SUI_RPC_URL=
# SUI_PRIVATE_KEY should be the server wallet private key in hex or base64 (secret key bytes).
SUI_PRIVATE_KEY=
# Optional: default recipient address for server-side transfers
ADMIN_SUI_ADDRESS=

# Copy this file to .env and update values for local development